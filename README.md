# GA-Project-3-Backend

## Package instalation
Before editing or running the application, use `$ npm install` to add the following dependencies:

- express
- heroku
- mongoose
- body-parser
- node.js
- nodemon (optional)

Once complete add a .gitignore file, and add node_modules and package-lock.json to it.